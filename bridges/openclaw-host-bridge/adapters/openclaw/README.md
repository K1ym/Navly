# OpenClaw Adapter Skeleton

本目录预留给 OpenClaw host 适配层。

当前 milestone A 只允许：

- 记录未来将对接的 gateway / session / hook / tool surface
- 明确这里只做 adapter，不做 Navly 业务真相

当前**不**实现：

- OpenClaw patch
- 完整 gateway integration
- session restore 逻辑
- tool runtime 逻辑
