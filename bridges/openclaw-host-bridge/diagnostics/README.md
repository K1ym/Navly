# Diagnostics Backbone

本目录承载 host trace / diagnostics / shared trace linkage backbone。

当前 milestone B 已实现：

- `host_trace_event` backbone
- milestone B host trace bundle

当前**不**实现：

- operator diagnostics UI
- full outcome forwarding
- cross-module trace stitching beyond bridge-local bundle
