---
description: Use when the user asks for field coverage, schema alignment, or quality reporting. Call the `navly_quality_report` tool.
---

Use this skill for data-quality requests.

- Primary tool: `navly_quality_report`
- Capability: `navly.ops.quality_report`
- Inputs: `scope_ref`, `business_date`, `include_explanation`

Do not expose raw payload paths unless the capability explicitly returns them.
