# Data Platform Owner Contracts

本目录只存放 **data-platform owner contracts** 的 seed。

不在此定义跨模块 shared contracts；跨模块主定义权属于 `shared/contracts`。

当前 milestone A 只放 contract ownership / registry entry shape seed。
