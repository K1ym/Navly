# Warehouse Skeleton

本目录未来负责 canonical fact truth。

当前 milestone A 不实现 SQL / dbt 模型。

当前已在文件级别固定的 canonical landing：

- member insight:
  - `customer`
  - `customer_card`
  - `consume_bill`
  - `consume_bill_payment`
  - `consume_bill_info`
- finance summary prerequisites:
  - `recharge_bill`
  - `recharge_bill_payment`
  - `recharge_bill_sales`
  - `recharge_bill_ticket`
  - `account_trade`
