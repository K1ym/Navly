# auth-kernel local docs

本目录仅用于 `platforms/auth-kernel/` 的局部实现说明。

当前 authoritative spec 仍以 `docs/specs/navly-v1/auth-kernel/` 为准；本地实现已经推进到 milestone D backbone，并补齐：

- `session_grant_snapshot`
- `audit_event_ledger`
- `decision_trace_view`
- downstream outcome linkage 的 fail-closed owner-side shape
