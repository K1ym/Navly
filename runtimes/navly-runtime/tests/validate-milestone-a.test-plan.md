# Validate Milestone A Test Plan

- 运行 `scripts/validate-milestone-a.sh`
- 验证目录骨架完整
- 验证 route registry 采用 namespaced capability/service IDs
- 验证 runtime shared consumption manifest 覆盖 interaction + access + readiness + service
- 验证未引入 legacy glue 命名
