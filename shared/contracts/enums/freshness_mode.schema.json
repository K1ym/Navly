{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "freshness_mode",
  "description": "Frozen shared enum for readiness freshness mode.",
  "type": "string",
  "enum": [
    "latest_usable",
    "strict_date"
  ]
}
