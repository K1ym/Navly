{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "readiness_status",
  "description": "Frozen shared enum for capability readiness status.",
  "type": "string",
  "enum": [
    "ready",
    "pending",
    "failed",
    "unsupported_scope"
  ]
}
